要在 Docker 这样没有图形界面的无头环境（Headless）中完成 Antigravity CLI（agy）的 Fallback
登录，你可以分为**“手动测试操作”和“代码集成操作”**两步来看。

以下是具体的命令和操作流程：

一、 纯命令行手动操作（在终端中走通流程）

要让用户“粘贴代码”回终端，你的 Docker 容器必须以**交互模式（Interactive + TTY）**运行。

1. 交互式进入 Docker 容器 如果启动新容器：

docker run -it --name agy-test ubuntu:latest /bin/bash

如果进入正在运行的容器：

docker exec -it <你的容器ID或名称> /bin/bash

(注：必须带 -it 参数，否则无法分配伪终端，你将无法输入验证码。)

2. 触发 Antigravity CLI 的回退登录 在容器内部直接运行：

agy

💡 进阶技巧：因为 CLI 的判定机制是“检测到 SSH 会话时打印授权 URL”。如果你在普通 Docker 容器里直接运行 agy
导致它强行去调用浏览器而报错，你可以伪造一个 SSH 环境变量强制触发打印链接的 Fallback 模式，命令如下：

SSH_CONNECTION=1 agy

3. 复制链接与粘贴验证码 执行上述命令后，终端会暂停并打印类似如下内容：

No local browser detected (Remote/SSH session).
Please open the following URL in a browser to authorize Google Antigravity:
https://accounts.google.com/o/oauth2/auth?client_id=xxxx...

Enter your authorization code: 

此时，你在本地电脑浏览器中打开该 https://... 链接，授权你的 Google 账号，页面会显示一串如 4/0AeaYI...
的代码。你将代码复制并粘贴回 Docker 终端，按下 Enter 键，即可完成配置。

二、 如果要内置到你的软件中，用什么“命令”实现？

如果你要把它内置成一个软件产品（比如 Web 应用或桌面端），你不能指望用户去敲 Docker 命令。你的后端服务需要用代码来模拟上述的人工输入过程。

由于 agy 需要真实的终端交互，传统的 stdin/stdout 管道（如 subprocess.Popen）可能会因为缓冲问题阻塞。推荐使用 Expect
类工具或 PTY（伪终端）。

方案 A：使用 Python 后端（借助 pexpect 库）

如果你用 Python 做后端软件，可以通过如下代码截获 URL 并“粘贴”代码：

import pexpect

# 1. 启动 agy 命令，并伪装 SSH 环境以确保触发打印 URL
env = {"SSH_CONNECTION": "1"}
# 启动伪终端进程
child = pexpect.spawn('agy', env=env, encoding='utf-8')

# 2. 等待 agy 打印出要求输入代码的提示（假设提示包含 'code:'）
child.expect(['code:', pexpect.EOF, pexpect.TIMEOUT], timeout=10)

# 3. 截获并提取打印在屏幕上的 URL
output = child.before
print("【系统日志】请将以下 URL 发送给前端界面显示：\n", output)

# --- 此时，前端通过 WebSocket 或 API 拿到 URL 给用户展示，用户输入 Code ---
# 假设这是前端传回来的代码
user_auth_code = "4/0AeaYI....YOUR_CODE_HERE" 

# 4. 执行“粘贴并回车”的命令操作
child.sendline(user_auth_code)

# 登录成功，进程继续往下走
child.expect(pexpect.EOF)

方案 B：使用 Node.js 后端（借助 node-pty 库）

如果是 Node 环境，可以使用 node-pty 模拟真实的终端操作命令：

const pty = require('node-pty');

// 1. 唤起 CLI 进程并注入环境变量
const agyProcess = pty.spawn('agy', [], {
  name: 'xterm-color',
  cols: 80,
  rows: 30,
  env: { ...process.env, SSH_CONNECTION: '1' }
});

// 2. 监听终端输出的数据流
agyProcess.onData((data) => {
  // 如果输出中包含了 Google OAuth 的链接，提取它并推送给前端
  if (data.includes('accounts.google.com')) {
      console.log('截获到登录链接，通过 WebSocket 发送给前端UI...');
      // sendToFrontend(data);
  }
});

// 3. 当你在前端界面获取到用户填写的 Auth Code 后，执行写入命令
function onUserSubmitAuthCode(code) {
  // \r 代表回车键（Enter），等同于用户手动在终端中敲入代码并回车
  agyProcess.write(code + '\r');
}

总结

1.  手工交互命令：必须加 -it 参数运行容器，可附加 SSH_CONNECTION=1 agy 强制打印链接。
2.  软件集成命令：不是用简单的单行 bash 脚本，而是要通过 pexpect.spawn()（Python）或
    pty.spawn()（Node.js）来启动进程，截取屏幕输出的 URL 给前端，再调用 .sendline(code) /
    .write(code + '\r') 命令将代码回传。
